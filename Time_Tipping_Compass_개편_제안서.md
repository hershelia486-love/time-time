# Time Tipping Compass 다음 개편 제안서

## 결론 요약

현재 앱은 **할 일·일정·루틴·프로젝트·도구·성찰을 각각 기록하고 관리하는 기본 기능**은 충분히 갖추고 있습니다. 다만 사용자가 실제로 매일 열었을 때 “지금 무엇을 해야 하는가”, “작은 습관이 오늘 남아 있는가”, “계획이 바뀌면 어디로 옮겨야 하는가”를 한 화면에서 빠르게 판단하는 흐름은 더 강화할 필요가 있습니다.

다음 개편의 핵심은 기록 기능을 더 많이 쌓는 것이 아니라, 현재의 분리된 기능을 **오늘의 실행 보드** 하나로 연결하는 것입니다. 프로젝트와 도구는 계속 독립적으로 운영하되, 오늘 화면에서 서로의 다음 행동을 한눈에 보이게 하겠습니다. 또한 날짜를 버튼으로만 바꾸는 방식에서 발전해, 길게 누르고 끌어 놓는 방식으로 일정·할 일·루틴을 재배치할 수 있게 하겠습니다.

> **개편 원칙:** 메인은 “판단과 실행”, 각 탭은 “전체 목록과 상세 편집”을 담당합니다.

## 1. 이미 구현된 것과 보완이 필요한 것

사용자께서 말씀하신 항목 중 상당수는 현재 버전에 이미 들어가 있습니다. 다만 범위가 제한적이거나 화면 구조상 충분히 드러나지 않는 부분이 있어, 다음 개편에서 더 명확하게 보완하겠습니다.

| 요구 사항 | 현재 상태 | 보완 방향 |
|---|---|---|
| 완료한 할 일·루틴의 금색 줄 | 구현됨. 완료 항목에 금색 줄과 `✓ 완료` 스탬프 표시 | 오늘 보드·루틴 전용 탭·캘린더의 모든 표현 방식에 같은 완료 표시를 일관 적용 |
| 계획·진행·완료 상태 변경 | 구현됨. 할 일·루틴 카드에서 드롭다운 변경 가능 | 드래그로도 상태 칸을 옮길 수 있도록 확장 |
| 할 일 당기기·미루기 | 구현됨. 버튼으로 하루 단위 이동, 미루기는 확인창 제공 | 주간 보드·월간 캘린더에서 끌어 놓기로 날짜 변경 지원 |
| 요일별 캘린더 가로·세로 보기 | 구현됨 | 주간 보드를 메인과 캘린더가 공유하고, 드롭 영역을 명확히 표시 |
| 요일 고정 루틴 | 구현됨. 단일 요일 선택 방식 | 월·수·금처럼 **여러 요일을 동시에 선택**하도록 데이터와 화면 개편 |
| 비고정 루틴 선택·해제 | 구현됨. 날짜별 선택 패널 제공 | 드래그로 다른 날짜로 옮기고, 작은 습관을 빠르게 추가할 수 있게 개선 |
| 모든 루틴 목록 | 성찰 탭 안의 주기별 접이식 목록으로 제공 | **루틴 전용 탭**으로 분리해 전체·오늘·요일·주기·상태별 탐색 제공 |
| 메인 통합 확인 | 오늘 화면에 핵심 할 일·루틴·영역 요약만 일부 제공 | 프로젝트·도구·성찰·일정을 “오늘 실행 보드”에서 연결 |
| 드래그 이동·정렬 | 미구현 | 모바일 길게 누르기 기반의 재정렬·날짜 이동·상태 이동 구현 |

## 2. 현재 구조에서 가장 큰 단점

현재 구조의 가장 큰 단점은 기능이 부족해서가 아니라, 사용자의 “오늘 실행” 관점에서 각 정보가 여러 탭으로 나뉘어 있다는 점입니다. 프로젝트는 프로젝트 탭, 도구는 도구 탭, 성찰은 성찰 탭, 일정은 캘린더 탭에 각각 잘 저장되지만, 사용자가 아침에 앱을 열었을 때 이 정보가 **하나의 행동 순서**로 자동 연결되지는 않습니다.

또한 고정 루틴이 현재 단일 요일만 지원하므로 “월·수·금 운동”, “화·목 독서”, “평일 약 복용”처럼 자주 쓰는 반복 패턴을 자연스럽게 표현하기 어렵습니다. 날짜 변경도 버튼을 여러 번 눌러야 하므로, 계획을 빠르게 재배치하는 모바일 플래너의 느낌이 약합니다.

| 보완이 필요한 지점 | 왜 문제인가 | 개편 목표 |
|---|---|---|
| 메인 화면의 정보 분절 | 오늘 일정과 프로젝트·도구·성찰의 맥락이 분리됨 | “오늘 해야 할 것”으로 재조합 |
| 루틴의 단일 요일 제한 | 복수 요일 습관을 직접 표현할 수 없음 | 다중 요일 선택과 날짜별 실행 상태 |
| 루틴 전용 탐색 부재 | 모든 습관을 한 곳에서 관리하기 어려움 | 전용 탭과 필터·검색·오늘 보기 제공 |
| 수동 날짜 변경 | 반복적으로 버튼을 눌러야 함 | 끌어 놓기로 날짜·순서·상태 이동 |
| 프로젝트와 실행 항목 연결 부족 | 프로젝트 결과와 오늘 행동의 연결이 약함 | 프로젝트별 다음 행동과 진행 상황 표시 |
| 도구·성찰의 실행 전환 부족 | 생각은 기록되지만 바로 행동이 되지 않을 수 있음 | “이 기록을 할 일로 만들기” 연결 버튼 |

## 3. 프로젝트 탭과 도구 탭의 관계

**프로젝트 탭과 도구 탭은 데이터와 역할 면에서 별개로 운영하는 것이 맞습니다.** 프로젝트는 “완료 가능한 결과물”을 관리하는 공간이고, 도구는 “생각을 정리하고 판단을 돕는 질문 프레임”을 사용하는 공간입니다. 두 탭을 하나로 합치면 프로젝트가 단순 메모처럼 보이거나, 도구가 할 일 목록처럼 보일 위험이 있습니다.

다만 독립은 고립을 뜻하지 않습니다. 프로젝트·도구·성찰에서 나온 결과가 오늘의 행동으로 전환될 수 있도록 연결해야 합니다. 이 연결이 새 메인 화면의 핵심 역할입니다.

| 출발 지점 | 독립 탭에서 하는 일 | 오늘 화면으로 연결되는 결과 |
|---|---|---|
| 프로젝트 | 결과와 완료 기준을 정의 | 해당 프로젝트의 **다음 행동 1개**와 진행률 |
| 도구 | 질문에 답하고 생각을 정리 | “이 기록을 할 일로 만들기”로 만든 다음 행동 |
| 성찰 | 잘한 선택·마찰·더 좋은 질문 기록 | 내일 또는 이번 주의 조정 행동 |
| 캘린더 | 날짜와 시간에 행동 배치 | 오늘의 시간 순서와 미완료 항목 |
| 루틴 | 작은 행동의 반복 관리 | 오늘 실행할 고정·비고정 습관 |

## 4. 권장 탭 구조

휴대폰 화면에서 하단 탭을 6개 이상 두면 글자와 터치 영역이 지나치게 작아집니다. 따라서 **하단 5탭을 유지**하되, 루틴을 독립시키고 도구·성찰은 같은 상위 영역 안에서 명확한 보조 탭으로 분리하는 구성을 권장합니다.

```mermaid
flowchart LR
    H[오늘\n실행 보드] --> C[캘린더\n월·주·드래그]
    C --> R[루틴\n전체 습관]
    R --> P[프로젝트\n결과·다음 행동]
    P --> M[생각\n도구 | 성찰]
    M --> H
```

| 하단 탭 | 목적 | 내부 구성 |
|---|---|---|
| **오늘** | 모든 정보를 오늘의 실행 순서로 통합 | 오늘 보드, 빠른 수집, 일정·루틴·프로젝트·도구·성찰 요약 |
| **캘린더** | 날짜 기반 계획의 전체 보기와 이동 | 월간, 요일별 주간, 가로/세로, 필터, 드래그 날짜 이동 |
| **루틴** | 습관의 전체 목록과 반복 규칙 관리 | 오늘 루틴, 전체 목록, 요일 필터, 주기 필터, 다중 요일 편집 |
| **프로젝트** | 결과 중심의 장기 실행 관리 | 프로젝트 카드, 완료 기준, 다음 행동, 연결된 일정·할 일 |
| **생각** | 도구와 성찰을 독립적인 두 보조 탭으로 제공 | `도구` / `성찰` 세그먼트 탭, 기록을 할 일로 전환 |

이 구조에서 도구와 성찰은 같은 “생각” 공간에 들어가지만, 각자 별개의 화면·데이터·기록 흐름을 유지합니다. 프로젝트 역시 완전히 독립된 관리 탭을 유지합니다. 결과적으로 하단 내비게이션은 과밀해지지 않고, 사용자께서 요청하신 루틴 전용 탭과 프로젝트·도구의 독립성도 보장됩니다.

## 5. 메인 화면: 오늘의 실행 보드 배치안

메인 화면은 모든 목록을 그대로 늘어놓는 페이지가 아니라, 오늘 필요한 정보를 **시간 순서와 행동 우선순위**로 압축한 페이지여야 합니다. 사용자가 아침·낮·저녁에 여러 번 열어도 부담스럽지 않도록, 위쪽은 실행 중심으로 단순하게 만들고 아래쪽은 필요할 때 펼쳐 보는 요약으로 배치합니다.

```text
┌──────────────────────────────────┐
│ 8월 21일 금요일 · 오늘의 나침반     │
│ [빠른 수집 +] [전체 달성률 42%]     │
├──────────────────────────────────┤
│ ① 지금 한 가지                     │
│ ┌ 핵심 프로젝트 20분 점검 ────────┐ │
│ │ 경력 · 중요 · 진행 │ 완료 / 이동 │ │
│ └────────────────────────────────┘ │
├──────────────────────────────────┤
│ ② 오늘 시간표                       │
│ 09:00 일정   12:30 루틴   19:00 할 일 │
│ 드래그하여 순서·날짜 변경             │
├──────────────────────────────────┤
│ ③ 작은 습관  3/5                    │
│ □ 물 마시기  □ 독서 20분  ✓ 스트레칭 │
│ [오늘의 루틴 모두 보기]               │
├──────────────────────────────────┤
│ ④ 프로젝트 다음 행동                  │
│ 90일 나침반 · 다음: 가치 시간 확정     │
├──────────────────────────────────┤
│ ⑤ 오늘의 도구 한 장                  │
│ 더 좋은 질문하기 · 기록 계속하기       │
├──────────────────────────────────┤
│ ⑥ 저녁 성찰                          │
│ 잘한 선택 / 마찰 / 내일의 질문         │
├──────────────────────────────────┤
│ 지난 기록 · 예정 · 전체 목록  [펼치기] │
└──────────────────────────────────┘
```

### 5.1 상단: 빠른 수집과 한 가지 행동

가장 상단에는 날짜와 오늘 달성률을 짧게 보여 주고, 오른쪽에 **빠른 수집 `+`** 버튼을 둡니다. 작은 습관, 갑자기 떠오른 걱정, 해야 할 일은 이 버튼으로 먼저 한 줄만 기록합니다. 이 기능은 향후 수집함(Inbox)으로 연결합니다. 당장 분류를 강제하지 않기 때문에 생각을 놓치지 않게 해 줍니다.

그 아래에는 “지금 한 가지” 카드만 크게 둡니다. 이 카드는 중요도, 날짜, 진행 상태, 프로젝트 연결 여부를 고려해 자동 추천하되, 사용자가 직접 바꾸거나 끌어 놓을 수 있습니다. 이 카드가 메인의 가장 중요한 시선 지점입니다.

### 5.2 중간: 오늘 시간표와 작은 습관

메인 중간에는 오늘 날짜의 일정·할 일·루틴을 하나의 세로 타임라인으로 보여 줍니다. 시간 없는 할 일은 “오늘 중” 영역에 표시하고, 루틴은 고정·선택 여부를 작은 표시로 구분합니다. 작은 습관은 별도의 체크 줄로 묶어 “3/5 완료”처럼 표시합니다. 이 영역은 사용자가 자주 움직이는 곳이므로, 드래그 조작의 첫 적용 대상입니다.

### 5.3 하단: 프로젝트·도구·성찰 연결 카드

프로젝트는 전체 목록을 메인에 반복하지 않고, **오늘과 연결된 프로젝트의 다음 행동 하나**만 노출합니다. 도구는 무작위 추천 또는 마지막으로 열었던 도구 한 장을 보여 주며, 성찰은 저녁 시간에만 강조되도록 시간대에 따라 “오늘의 저녁 성찰” 카드로 노출합니다. 각 카드를 누르면 해당 독립 탭으로 이동합니다.

이 방식은 모든 기능을 메인에서 볼 수 있게 하면서도, 메인이 또 하나의 긴 목록이 되는 것을 막습니다.

## 6. 루틴 전용 탭 설계

루틴은 성찰 탭 안의 하위 기능이 아니라, 매일 반복되는 행동을 관리하는 독립 영역이 되어야 합니다. 루틴 전용 탭에는 오늘 실행할 습관과 전체 습관 목록을 모두 제공합니다.

| 화면 영역 | 내용 | 사용자 행동 |
|---|---|---|
| 오늘의 루틴 | 오늘 실행할 고정·비고정 루틴 | 완료, 진행, 미루기, 드래그 순서 변경 |
| 주간 요일 보드 | 월~일별 루틴 배치 | 여러 요일 선택, 다른 요일로 끌어 이동 |
| 전체 루틴 목록 | 활성·보류를 포함한 모든 루틴 | 검색, 주기·상태·요일 필터, 수정·삭제 |
| 주기 성과 | 주간·월간·연간 목표와 완료율 | 목표 변경, 성과 확인 |
| 새 루틴 | 이름·목표·주기·고정 방식 입력 | 다중 요일 체크, 비고정 일정 선택 |

### 6.1 다중 요일 고정 루틴

현재는 `weekday` 한 개만 저장합니다. 이를 아래처럼 변경합니다.

| 구분 | 현재 | 개편 후 |
|---|---|---|
| 고정 요일 데이터 | `weekday: 0` | `fixedWeekdays: [0, 2, 4]` |
| 편집 UI | 요일 드롭다운 하나 | 월·화·수·목·금·토·일 체크 버튼 7개 |
| 예시 | “월요일 아침 계획” | “월·수·금 스트레칭”, “평일 물 마시기” |
| 주간 표시 | 한 요일에만 자동 표시 | 선택된 모든 요일에 자동 표시 |

고정 루틴의 시간 또는 위치도 필요하다면 이후 `timeSlot`과 `sortOrder`를 추가합니다. 이 데이터가 있으면 같은 날 여러 습관을 사용자가 원하는 순서로 배치할 수 있습니다.

## 7. 드래그 기반 조작 설계

드래그 기능은 단순한 시각 효과가 아니라, 계획을 다시 짜는 핵심 조작이어야 합니다. 특히 휴대폰에서는 일반 클릭과 드래그가 충돌하기 쉬우므로, 카드 전체를 바로 끄는 대신 **드래그 핸들을 350ms 이상 길게 누르면 이동 모드가 시작**되도록 설계합니다. 일반 탭은 상세 편집, 길게 누르기는 이동으로 구분합니다.

| 사용자의 드래그 | 드롭 위치 | 변경되는 값 | 결과 |
|---|---|---|---|
| 오늘 할 일 카드 | 같은 날짜의 위·아래 | `sortOrder` | 오늘 목록의 우선순위 순서 변경 |
| 일정·할 일 카드 | 다른 날짜·주간 칸 | `date` | 당기기·미루기·날짜 재배치 |
| 일정·할 일 카드 | 계획·진행·완료 상태 칸 | `status` | 버튼 없이 상태 이동, 완료 시 금색 표시 |
| 비고정 루틴 카드 | 다른 날짜 | 해당 날짜 선택 기록 | 기존 날짜에서는 해제, 새 날짜에 선택 |
| 고정 루틴 카드 | 다른 요일 | `fixedWeekdays` | 해당 요일 목록을 추가·삭제 |
| 프로젝트 다음 행동 | 오늘 보드 | `item.date`, `projectId` | 프로젝트의 다음 행동을 오늘 실행으로 배치 |

### 7.1 드래그 시 사용자 경험 규칙

1. 이동 가능한 카드에는 작은 `⋮⋮` 핸들을 표시합니다.
2. 길게 누르면 카드가 살짝 떠오르고, 이동 가능한 날짜·상태 영역만 강조합니다.
3. 드롭하기 전에는 “8월 24일 월요일로 이동”처럼 목적지를 짧게 보여 줍니다.
4. 완료 상태로 놓으면 금색 줄과 `✓ 완료` 스탬프를 즉시 표시합니다.
5. 미루기 드래그는 실수 방지를 위해 날짜를 오늘보다 뒤로 옮길 때 한 번 확인합니다. 단순 순서 변경에는 확인을 요구하지 않습니다.
6. 드래그를 사용하기 어려운 환경을 위해 기존의 수정, 상태 드롭다운, 하루 당기기·미루기 버튼은 계속 유지합니다.

## 8. 필요한 데이터 모델 개선

현재 구조는 단순하고 안정적이지만, 다중 요일 루틴과 날짜별 드래그를 제대로 처리하려면 다음 필드를 추가해야 합니다. 기존 저장 데이터가 사라지지 않도록, 기존 `weekday` 값은 최초 실행 시 `fixedWeekdays: [weekday]`로 자동 변환하는 마이그레이션을 적용합니다.

```javascript
// 일정·할 일 확장
{
  id,
  title,
  type,                 // task | event
  date,                 // YYYY-MM-DD
  area,
  status,               // planned | progress | completed
  priority,
  projectId: null,      // 선택 연결
  sortOrder: 0,         // 날짜 안에서의 드래그 순서
  timeSlot: null        // 선택 시간, 예: "09:00"
}

// 루틴 템플릿 확장
{
  id,
  title,
  cadence,
  target,
  active,
  scheduleType: "fixed" | "flexible",
  fixedWeekdays: [0, 2, 4],
  scheduledDates: ["2026-08-24"],
  statusByPeriod: {},
  statusByDate: {},
  sortOrderByDate: {},
  completions: []
}
```

드래그 대상의 종류가 늘어날수록 모든 데이터를 한 배열에 합치기보다는, 화면에서만 일정·할 일과 루틴 발생분을 **통합 타임라인 항목**으로 변환하는 방식을 권장합니다. 저장 구조는 각각 유지하므로 프로젝트·루틴·성찰의 역할이 섞이지 않고, 화면에서는 사용자가 원하는 한 줄 시간표로 보일 수 있습니다.

## 9. 구현 우선순위

한 번에 모든 화면을 바꾸면 기존 기록의 안정성을 해칠 수 있으므로 아래 순서로 진행하는 것이 좋습니다.

| 단계 | 구현 범위 | 완료 기준 |
|---:|---|---|
| 1 | 루틴 전용 탭, 다중 요일 선택, 기존 데이터 마이그레이션 | 월·수·금 등 복수 요일 루틴이 주간 캘린더와 오늘 화면에 표시 |
| 2 | 오늘 실행 보드, 프로젝트·도구·성찰 요약 카드, 빠른 수집함 | 메인에서 오늘의 일정·루틴·프로젝트 다음 행동·도구·성찰을 확인 |
| 3 | 할 일·일정의 드래그 순서 변경과 날짜 이동 | 오늘 보드와 주간 캘린더에서 끌어 놓기로 날짜·순서 변경 |
| 4 | 상태 칸 드롭, 루틴 날짜 이동, 고정 요일 이동 | 드래그로 계획·진행·완료 및 루틴 배치 변경 |
| 5 | 수집함 분류, 막힘 상태, 알림 시간 | 생각 포착·실행 불가 사유·리마인더 완성 |

## 10. 추천 결정

가장 먼저는 **루틴 전용 탭 + 다중 요일 체크 + 메인 실행 보드**를 구현하는 것이 맞습니다. 작은 습관을 놓치지 않으려면 습관이 성찰 화면 안에 묻히지 않고, 오늘 화면과 루틴 탭에서 모두 보여야 하기 때문입니다. 그 다음으로 일정·할 일의 드래그 이동을 붙이면 계획을 바꾸는 일이 훨씬 빠르고 자연스러워집니다.

프로젝트와 도구는 계속 별개로 운영하되, 프로젝트에서는 다음 행동을 만들고 도구·성찰에서는 생각을 할 일로 전환하는 연결 버튼을 추가하겠습니다. 이렇게 하면 각 기능의 정체성은 유지하면서도, 사용자는 메인 화면 하나에서 오늘의 삶 전체를 관리할 수 있습니다.
